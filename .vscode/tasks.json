{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-overlay",
            "command": "msbuild",
            "type": "process",
            "args": ["LaunchpadOverlay.sln"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": "$tsc"
        }
    ]
}
