/*
    A Simple Module is a module which has an info file and defines services and parameters.

    Modules can hook into events by declaring services with the "event_subscriber" tag which
    contain a "GetEventSubscribers" method.
*/
class SimpleModule extends ModuleBase {
    
}
