/*
    Runtime config accesses existing parameters and stores changes to parameters,
    but these changes are not persisted anywhere after exiting the application.
*/
class RuntimeConfig extends ConfigBase {

}
